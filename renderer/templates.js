function chromeMarkup() {
  const menuItems = {
    file: [['new-character', 'New character', 'Ctrl+N'], ['new-gallery', 'New collection', ''], ['rename-gallery', 'Rename collection', ''], ['delete-gallery', 'Delete collection', ''], ['divider'], ['import', 'Import collection…', ''], ['export', 'Export collection…', ''], ['open-folder', 'Open archive folder', ''], ['save-library', 'Save archive', 'Ctrl+S'], ['divider'], ['exit', 'Exit', 'Alt+F4']],
    edit: [['undo-dna', 'Undo DNA edit', 'Ctrl+Z'], ['redo-dna', 'Redo DNA edit', 'Ctrl+Y'], ['batch-select', 'Select multiple characters', 'Ctrl+Click'], ['focus-search', 'Find a character', 'Ctrl+F'], ['paste-portrait', 'Paste portrait from clipboard', 'Ctrl+V'], ['manage-record', 'Manage selected record', ''], ['edit-note', 'Edit notes', ''], ['open-dna', 'Open DNA workbench', '']],
    view: [['view-cards', 'Portrait cards', ''], ['view-table', 'Compact list', '']],
    help: [['help-hint', 'New character', 'Ctrl+N'], ['help-hint', 'Find a character', 'Ctrl+F'], ['help-hint', 'Save archive', 'Ctrl+S'], ['help-hint', 'Duplicate character', 'Ctrl+D'], ['help-hint', 'Export collection', 'Ctrl+E'], ['help-hint', 'Paste portrait or DNA', 'Ctrl+V'], ['help-hint', 'Copy selected DNA', 'Ctrl+C'], ['help-hint', 'Undo / redo DNA', 'Ctrl+Z / Ctrl+Y'], ['help-hint', 'Manage record', 'F2'], ['help-hint', 'Add to selection', 'Ctrl+Click'], ['help-hint', 'Delete selection', 'Delete']],
  };
  const menu = state.activeMenu;
  const popover = menu ? `<div class="chrome-menu" data-menu-popover="${menu}">${menuItems[menu].map(([actionName, label, shortcut]) => actionName === 'divider' ? '<div class="chrome-menu-divider"></div>' : `<button data-action="${actionName}"><span>${label}</span>${shortcut ? `<kbd>${shortcut}</kbd>` : ''}</button>`).join('')}</div>` : '';
  return `<header class="window-chrome"><div class="chrome-title"><span class="chrome-seal">III</span><strong>The Bloodline Index</strong></div><nav class="chrome-nav"><button class="${menu === 'file' ? 'active' : ''}" data-menu="file">File</button><button class="${menu === 'edit' ? 'active' : ''}" data-menu="edit">Edit</button><button class="${menu === 'view' ? 'active' : ''}" data-menu="view">View</button><button class="${menu === 'help' ? 'active' : ''}" data-menu="help">Help</button></nav><div class="chrome-drag-region"></div>${popover}</header>`;
}

function contextMenuMarkup() {
  const menu = state.contextMenu;
  if (!menu) return '';
  const batch = menu.type === 'batch';
  const galleryBatch = menu.type === 'gallery-batch';
  const character = menu.type === 'character' ? getCharacters().find((item) => item.id === menu.id) : null;
  const heading = menu.type === 'collection' ? menu.name : galleryBatch ? `${menu.names.length} COLLECTIONS SELECTED` : batch ? `${menu.ids.length} RECORDS SELECTED` : character?.name;
  const items = menu.type === 'collection'
    ? [['rename', icon('edit'), 'Rename'], ['duplicate', icon('copy'), 'Duplicate'], ['batch-select', icon('check'), 'Batch select'], ['divider'], ['delete', icon('trash'), 'Delete', 'danger']]
    : galleryBatch
      ? [['delete-selected-galleries', icon('trash'), 'Delete selected', 'danger'], ['divider'], ['cancel-gallery-batch', icon('close'), 'Cancel selection']]
    : batch
      ? [['transfer', icon('arrow'), 'Move / Copy'], ['divider'], ['delete-batch', icon('trash'), 'Delete selected', 'danger']]
      : [['manage', icon('edit'), 'Manage record'], ['customize-appearance', icon('palette'), 'Customize appearance'], ['favorite', icon('star'), state.favorites.has(menu.id) ? 'Remove favorite' : 'Add favorite'], ['divider'], ['copy-dna', icon('copy'), 'Copy DNA'], ['paste-dna', icon('download'), 'Paste DNA'], ['transfer', icon('arrow'), 'Move / Copy'], ['duplicate', icon('copy'), 'Duplicate'], ['divider'], ['delete', icon('trash'), 'Delete', 'danger']];
  return `<div class="context-menu" role="menu" style="left:${menu.x}px;top:${menu.y}px"><p>${escapeHtml(heading || '')}</p>${items.map(([actionName, itemIcon, label, className]) => actionName === 'divider' ? '<div class="context-menu-divider"></div>' : `<button class="${className || ''}" data-context-action="${actionName}" role="menuitem">${itemIcon}<span>${label}</span></button>`).join('')}</div>`;
}

function cardMarkup(character, index, reorderable = false) {
  const favorite = state.favorites.has(character.id);
  const selected = !state.batchMode && state.activeId === character.id;
  const batchSelected = state.selectedCharacterIds.has(character.id);
  const variantCount = character.images?.length || (state.preview ? character.variants || 0 : 0);
  const coverIndex = coverVariantIndex(character);
  return `<article class="character-card ${selected ? 'selected' : ''} ${state.batchMode ? 'batch-selectable' : ''} ${batchSelected ? 'batch-selected' : ''}" data-character-id="${escapeHtml(character.id)}" tabindex="0"${state.batchMode ? ` aria-pressed="${batchSelected}"` : ''}${reorderable ? ' draggable="true"' : ''}><div class="card-portrait">${portraitMarkup(character, 'card', coverIndex)}${state.batchMode ? `<span class="batch-check">${batchSelected ? icon('check') : ''}</span>` : ''}<div class="card-topline"><span class="status-dot ${character.status}"></span>${character.status === 'ready' ? 'DNA READY' : 'DRAFT'}<button class="icon-button favorite ${favorite ? 'active' : ''}" data-action="favorite" title="${favorite ? 'Remove from favorites' : 'Add to favorites'}">${icon('star')}</button></div><div class="card-index">${String(index + 1).padStart(2, '0')}</div>${variantCount > 1 ? `<div class="card-bottom"><div class="card-cycle"><button data-cycle-portrait="-1" title="Previous cover portrait" aria-label="Previous cover portrait">‹</button><span>${coverIndex + 1}/${variantCount}</span><button data-cycle-portrait="1" title="Next cover portrait" aria-label="Next cover portrait">›</button></div></div>` : ''}</div><div class="card-info"><div><h3>${escapeHtml(character.name)}</h3>${character.title ? `<p>${escapeHtml(character.title)}</p>` : ''}</div><button class="more-button" data-action="more" title="More actions">${icon('more')}</button></div><div class="tag-row">${character.tags.slice(0, 3).map((tag) => `<span class="tag">${escapeHtml(tag)}</span>`).join('')}${character.tags.length > 3 ? `<span class="tag muted">+${character.tags.length - 3}</span>` : ''}</div></article>`;
}

function tableMarkup(character) {
  const favorite = state.favorites.has(character.id);
  const batchSelected = state.selectedCharacterIds.has(character.id);
  const coverIndex = coverVariantIndex(character);
  const preview = imageUrlFor(character, coverIndex) ? `<span class="table-portrait-preview" aria-hidden="true">${portraitMarkup(character, 'list-preview', coverIndex)}</span>` : '';
  return `<button class="table-row ${!state.batchMode && state.activeId === character.id ? 'selected' : ''} ${state.batchMode ? 'batch-selectable' : ''} ${batchSelected ? 'batch-selected' : ''}" data-character-id="${escapeHtml(character.id)}"${state.batchMode ? ` aria-pressed="${batchSelected}"` : ''}><span class="table-avatar">${portraitMarkup(character, 'mini', coverIndex)}${preview}</span><span class="table-name"><strong>${escapeHtml(character.name)}</strong>${character.title ? `<small>${escapeHtml(character.title)}</small>` : ''}</span><span class="table-tags">${character.tags.slice(0, 2).map((tag) => `<span class="tag">${escapeHtml(tag)}</span>`).join('')}</span><span class="table-status ${character.status}">${character.status === 'ready' ? icon('check') + ' ready' : 'draft'}</span><span class="table-favorite ${favorite ? 'active' : ''}">${icon('star')}</span></button>`;
}

function sidebarMarkup(galleries) {
  const allCount = galleries.reduce((sum, gallery) => sum + gallery.characters.length, 0);
  const favoriteCount = [...state.favorites].filter((id) => galleries.some((gallery) => gallery.characters.some((character) => character.id === id))).length;
  return `<aside class="sidebar"><div class="brand"><div class="brand-seal">III</div><div><span>CRUSADER KINGS</span><strong>Bloodline<br/>Index</strong></div></div><div class="sidebar-rule"></div><button class="new-record" data-action="new-character">${icon('plus')} <span>New character</span><kbd>⌘ N</kbd></button><nav class="library-nav"><p class="nav-label">LIBRARY</p><button class="nav-item ${activeFilterCount() === 0 ? 'active' : ''}" data-filter="all"><span class="nav-symbol">◈</span><span>All characters</span><b>${allCount}</b></button><button class="nav-item ${state.filters.favorites ? 'active' : ''}" data-filter="favorites"><span class="nav-symbol">☆</span><span>Favorites</span><b>${favoriteCount}</b></button><button class="nav-item ${state.filters.dna === 'ready' ? 'active' : ''}" data-filter="ready"><span class="nav-symbol">◌</span><span>DNA ready</span></button><button class="nav-item ${state.filters.dna === 'drafts' ? 'active' : ''}" data-filter="drafts"><span class="nav-symbol">◍</span><span>Needs DNA</span></button></nav><nav class="collection-nav"><div class="nav-heading"><p class="nav-label">COLLECTIONS</p><div class="collection-actions"><button class="text-icon" data-action="rename-gallery" title="Rename selected collection"${state.preview ? ' disabled' : ''}>${icon('edit')}</button><button class="text-icon" data-action="delete-gallery" title="Delete selected collection"${state.preview ? ' disabled' : ''}>${icon('trash')}</button><button class="text-icon" data-action="new-gallery" title="New collection">${icon('plus')}</button></div></div>${galleries.map((gallery) => `<button class="collection-item ${state.activeGallery === gallery.name ? 'active' : ''}" data-gallery="${escapeHtml(gallery.name)}"${galleries.length > 1 && !state.preview ? ' draggable="true"' : ''}><span class="collection-dot"></span><span>${escapeHtml(gallery.name)}</span><b>${gallery.characters.length}</b></button>`).join('')}</nav><div class="sidebar-bottom"><button class="utility-link" data-action="import"><span>＋</span> Import collection</button><button class="utility-link" data-action="export"><span>↗</span> Export collection</button><div class="storage-note"><span class="pulse"></span><span>Local archive<br/><b>@huangfanglong</b></span></div></div></aside>`;
}

function inspectorMarkup(character) {
  if (!character) return `<aside class="inspector empty-inspector"><div class="inspector-empty-mark">◌</div><p class="eyebrow">NO RECORD SELECTED</p><p>Choose a portrait from the archive to inspect.</p><button class="outline-button" data-action="new-character">${icon('plus')} New character</button></aside>`;
  const favorite = state.favorites.has(character.id);
  const variantCount = character.images?.length || (state.preview ? character.variants || 0 : 0);
  const viewedVariant = state.selectedVariantIndex !== null && state.selectedVariantIndex < variantCount ? state.selectedVariantIndex : coverVariantIndex(character);
  return `<aside class="inspector"><div class="inspector-scroll"><div class="inspector-head"><div><p class="eyebrow">CHARACTER</p><span class="inspector-id">${escapeHtml(character.id.slice(0, 8).toUpperCase())}</span></div><div class="inspector-actions"><button class="icon-button ${favorite ? 'active' : ''}" data-action="favorite" title="Favorite">${icon('star')}</button><button class="icon-button" data-action="close-inspector" title="Close inspector">${icon('close')}</button></div></div><div class="inspector-portrait">${portraitMarkup(character, 'large', viewedVariant)}<div class="portrait-caption"><span>${variantCount} VARIANTS</span><span>${character.status === 'ready' ? 'DNA READY' : 'DNA DRAFT'}</span></div></div><div class="inspector-title"><div><h2>${escapeHtml(character.name)}</h2><input id="character-title" value="${escapeHtml(character.title)}" placeholder="..." aria-label="Character title" /></div><button class="text-icon" data-action="more">${icon('more')}</button></div><div class="inspector-tags">${characterTags(character).map((tag) => `<span class="tag">${escapeHtml(tag)}</span>`).join('')}</div><div class="variant-strip"><div class="variant-heading"><span>PORTRAIT VARIANTS</span><button data-action="add-variant">${icon('plus')} Add</button></div><div class="variant-row">${variantCount ? Array.from({ length: variantCount }, (_, index) => `<button class="variant-thumb ${state.focusContext === 'variant' && state.selectedVariantIndex === index ? 'selected' : ''}" data-variant="${index}" aria-label="Select portrait variant ${index + 1}">${portraitMarkup(character, 'thumb', index)}<span>${String(index + 1).padStart(2, '0')}</span></button>`).join('') : '<p class="variant-empty">No portrait variants yet</p>'}</div></div><div class="inspector-section"><div class="section-heading"><span>NOTES</span><button class="note-edit-button" data-action="edit-note" title="Edit notes" aria-label="Edit notes">${icon('edit')}</button></div><button class="field-note" data-action="edit-note" title="Edit notes">${character.note ? highlightedNoteMarkup(character.note) : 'Add notes or #tags.'}</button></div></div><div class="inspector-footer"><button class="outline-button" data-action="copy-dna">${icon('copy')} Copy DNA</button><button class="primary-button" data-action="open-dna">Open workbench ${icon('arrow')}</button></div></aside>`;
}

function mainMarkup(characters) {
  const queryLabel = state.query ? `Results for “${escapeHtml(state.query)}”` : state.preview ? 'A visual sample library' : 'Your personal character archive';
  const filterCount = activeFilterCount();
  return `<main class="main-content"><header class="topbar"><div class="breadcrumbs"><span>THE ARCHIVE</span><i>/</i><strong>${escapeHtml(state.activeGallery)}</strong></div><div class="top-actions"><button class="top-action" data-action="import">${icon('download')} Import</button><button class="top-action" data-action="export">${icon('arrow')} Export</button></div></header>${state.startupWarning ? `<div class="archive-warning" role="status">${icon('warning')}<span>${escapeHtml(state.startupWarning)}</span></div>` : ''}${state.preview ? `<div class="preview-banner"><span><b>PREVIEW COLLECTION</b> This is a visual sample of the new archive experience.</span><button data-action="start-blank">Start with an empty gallery ${icon('arrow')}</button></div>` : ''}<section class="archive-heading"><div><p class="eyebrow">${state.preview ? 'DESIGN PREVIEW' : 'CHARACTER ARCHIVE'}</p><h1>${escapeHtml(state.activeGallery)}</h1><p class="heading-subtitle">${queryLabel}</p></div><div class="archive-stats"><div><strong>${getCharacters().length}</strong><span>in collection</span></div><div class="stat-line"></div><div class="view-toggle"><button class="${state.view === 'cards' ? 'active' : ''}" data-view="cards" title="Card view">${icon('grid')}</button><button class="${state.view === 'table' ? 'active' : ''}" data-view="table" title="List view">${icon('list')}</button></div></div></section><div class="toolbar"><div class="search-box">${icon('search')}<input id="search-input" value="${escapeHtml(state.query)}" placeholder="Search a name, title, or tag…" /><kbd>⌘ F</kbd></div><div class="filter-control"><button class="filter-button ${state.filterPanelOpen ? 'active' : ''}" data-action="filters" aria-expanded="${state.filterPanelOpen}" aria-controls="filter-panel">${icon('sliders')} Filters${filterCount ? `<span class="filter-count">${filterCount}</span>` : ''}</button>${state.filterPanelOpen ? filterPanelMarkup(characters.length) : ''}</div>${sortControlMarkup()}</div><div class="filter-row"><button class="filter-chip ${filterCount === 0 ? 'active' : ''}" data-filter="all">All faces</button><button class="filter-chip ${state.filters.dna === 'ready' ? 'active' : ''}" data-filter="ready">DNA ready</button><button class="filter-chip ${state.filters.dna === 'drafts' ? 'active' : ''}" data-filter="drafts">Needs DNA</button><span class="filter-rule"></span><span class="results-note">${characters.length ? `Showing ${characters.length} of ${getCharacters().length}` : 'No matching records'}</span>${state.batchMode ? `<div class="batch-toolbar"><button data-action="select-all-visible">Select shown</button><span>${state.selectedCharacterIds.size} selected</span><button class="batch-delete" data-action="delete-batch"${state.selectedCharacterIds.size ? '' : ' disabled'}>Delete selected</button><button data-action="cancel-batch">Cancel</button></div>` : `<button class="batch-start" data-action="batch-select"${characters.length && !state.preview ? '' : ' disabled'}>${icon('check')} Select multiple</button>`}</div>${state.view === 'cards' ? `<section class="card-grid">${characters.length ? characters.map((character, index) => cardMarkup(character, index, characters.length > 1 && !state.preview && !state.batchMode)).join('') : emptyResultsMarkup()}</section>` : `<section class="table-view"><div class="table-header"><span>PORTRAIT</span><span>CHARACTER</span><span>TAGS</span><span>STATUS</span><span></span></div>${characters.length ? characters.map(tableMarkup).join('') : emptyResultsMarkup()}</section>`}<footer class="archive-footer"><span><span class="keyboard-dot"></span> Tip: paste an in-game screenshot anywhere to add a portrait</span><span>LOCAL ARCHIVE <b>·</b> v3.0 preview</span></footer></main>`;
}

function sortControlMarkup() {
  const options = SORT_OPTIONS;
  const selectedLabel = options.find(([value]) => value === state.sort)?.[1] || options[0][1];
  return `<div class="sort-control"><button class="sort-select ${state.sortMenuOpen ? 'active' : ''}" data-action="sort-menu" data-sort-value="${state.sort}" aria-haspopup="listbox" aria-expanded="${state.sortMenuOpen}" aria-controls="sort-menu"><span>Sort by</span><strong>${selectedLabel}</strong>${icon('chevron')}</button>${state.sortMenuOpen ? `<div class="sort-menu" id="sort-menu" role="listbox" aria-label="Sort characters">${options.map(([value, label]) => `<button class="${state.sort === value ? 'selected' : ''}" data-sort="${value}" role="option" aria-selected="${state.sort === value}"><span>${label}</span>${state.sort === value ? icon('check') : ''}</button>`).join('')}</div>` : ''}</div>`;
}

function filterPanelMarkup(matchCount) {
  const tags = availableTags();
  return `<section class="filter-panel" id="filter-panel" aria-label="Archive filters"><div class="filter-panel-head"><span>FILTER ARCHIVE</span><button data-action="clear-filters"${activeFilterCount() ? '' : ' disabled'}>Clear all</button></div><div class="filter-group"><span class="filter-label">DNA STATUS</span><div class="filter-segments" role="group" aria-label="DNA status"><button class="${state.filters.dna === 'all' ? 'active' : ''}" data-dna-filter="all">Any</button><button class="${state.filters.dna === 'ready' ? 'active' : ''}" data-dna-filter="ready">Ready</button><button class="${state.filters.dna === 'drafts' ? 'active' : ''}" data-dna-filter="drafts">Needs DNA</button></div></div><div class="filter-group"><span class="filter-label">RECORD</span><label class="filter-toggle"><input type="checkbox" data-favorite-filter${state.filters.favorites ? ' checked' : ''}/><span class="filter-checkbox">${state.filters.favorites ? icon('check') : ''}</span><span>Favorites only</span></label></div><div class="filter-group filter-tag-group"><span class="filter-label">TAGS</span><div class="filter-tag-list">${tags.length ? tags.map((tag) => `<label class="filter-tag-option ${state.filters.tags.has(tag) ? 'active' : ''}"><input type="checkbox" data-filter-tag value="${escapeHtml(tag)}"${state.filters.tags.has(tag) ? ' checked' : ''}/><span>#${escapeHtml(tag)}</span></label>`).join('') : '<p>No tags in this collection</p>'}</div></div><div class="filter-panel-foot"><span>${matchCount} match${matchCount === 1 ? '' : 'es'}</span><button data-action="close-filters">Done</button></div></section>`;
}

function emptyResultsMarkup() {
  return `<div class="empty-results"><span>⌁</span><h2>No faces found</h2><p>Try another name or clear the filters.</p><button class="outline-button" data-action="clear-search">Clear search and filters</button></div>`;
}
